# Search Filter Example

A text search widget should:
- accept user input;
- filter records by text;
- avoid external dependencies;
- keep state in the screen component unless the slice requires otherwise.
