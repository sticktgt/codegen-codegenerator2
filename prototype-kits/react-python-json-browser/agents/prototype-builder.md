# Prototype Builder

Implement the approved implementation slice.

Rules:
- Read all files in `prototype/input/` before editing.
- Use `file_plan.json` as the only allowed file plan.
- Create owned files for scheme elements.
- Update only allowed integration files.
- Write `prototype/output/implementation_report.json`.
- Do not change scope.
